# --- Rust ---
/target/
**/*.rs.bk
Cargo.lock.bak

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venvs/
venv/
.pytest_cache/
.ruff_cache/

# --- macOS ---
.DS_Store
._*

# --- Editors ---
.idea/
.vscode/
*.swp
*.swo

# --- Secrets / credentials ---
.env
.env.*
!.env.example
*.pem
*.key
*_rsa
*_rsa.pub
*.token
hf_token*
**/huggingface_token*
.admin_password
.whatsapp_bridge_token
whatsapp-auth/
**/whatsapp-auth/

# --- Large binary/generated artifacts ---
node_modules/
**/node_modules/
dist/
**/dist/
build/
coverage/
tmp/
*.sqlite
*.db

# Downloaded gov PDFs — regenerate via scripts/download_batch.py
survey/cdn_batch/
survey/ocr_output/

# CPT corpus assembly outputs — large, regenerable from source scripts
corpora/

# Pipeline outputs — regenerable from source PDFs + code
survey/corpus_chunks.jsonl
survey/bm25_index.json
survey/classification_batch.json
survey/classify.err

# Eval outputs (T9 on k2; local shouldn't have these but just in case)
eval/
*.log

# HF caches — on T9, not in repo
hf_cache/
**/hf_cache/

# MLX / model weights — on T9
checkpoints/
*.safetensors
*.gguf

# Demo capture artifacts kept outside the share branch.
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.mp4
/video_*/
/whatsapp-demo-local.png

# --- Local overrides ---
*.local
local_settings.*

# --- Session-local agent / launch artifacts (may contain instance IDs, tokens) ---
.aws-launches/
.claude/
.playwright-mcp/
